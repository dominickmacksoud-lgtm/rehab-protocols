<!DOCTYPE html>
<html lang="en">
<head>
  ${head}
</head>
<body>
  ${header}

  <main class="container rp-page">
    <nav class="rp-breadcrumb" aria-label="Breadcrumb">${breadcrumb}</nav>

    <h1>${h1}</h1>
    <p class="rp-intro">${intro}</p>

${comparison}

    <h2 id="all-protocols">All ${count} ${display} protocols</h2>
    <div class="rp-card-grid">
${cards}
    </div>

${related}

    <p class="rp-uplinks">
      <a href="${region_path}">All ${region} protocols</a>
      <span aria-hidden="true">&middot;</span>
      <a href="/protocols/">Browse all topics</a>
    </p>
  </main>

  ${footer}
  ${disclaimer}
</body>
</html>
